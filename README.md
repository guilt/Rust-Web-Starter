# Rust Web Starter

Basic Rust Web Server template based on `actix-web`. 

# Dependencies

We need `rust 1.45+` or above installed.

# Building

```bash
cargo build --release
```

# Running

```bash
set RUST_LOG=info
cargo run --release
```